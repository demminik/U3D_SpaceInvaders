namespace SpaceInvaders.Gameplay.Boosters {

    // TODO: need to think better over booster types (probably add subtype or split in some other way, not important for demo   )
    public enum EBoosterType {

        None = 0,

        Weapon_Triple,
    }
}